[package]
name = "tracker-tui"
version = "0.1.0"
edition = "2021"

[dependencies]
tracker-core = { path = "../tracker-core" }
ratatui = "0.28"
crossterm = "0.28"
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
hound = "3.5"
